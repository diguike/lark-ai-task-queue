# cron 模板(macOS/Linux,opt-in)。`crontab -e` 后粘贴,把 __ROOT__ 换成项目绝对路径。
# 注意:macOS 上机器睡眠时 cron 不触发;长期无人值守优先 launchd 或 daemon.sh。
# 每小时整点跑一轮:
0 * * * * cd __ROOT__ && /bin/bash scripts/cron-run.sh >> __ROOT__/logs/cron.log 2>&1
